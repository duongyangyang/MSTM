# Core ML
torch>=2.0.0
transformers>=4.40.0
datasets>=2.14.0
peft>=0.10.0
accelerate>=0.28.0

# Embedding / similarity (for heuristic consolidation baseline)
sentence-transformers>=2.7.0
scikit-learn>=1.3.0

# LLM API (for dataset generation via GPT-4o, Mem0-style baseline, eval reader)
openai>=1.30.0

# Data
huggingface_hub>=0.20.0

# Config
pyyaml>=6.0

# Evaluation
nltk>=3.8.0
numpy>=1.24.0
tiktoken>=0.7.0

# Optional — for experiment tracking
# wandb>=0.16.0
# tensorboard>=2.14.0